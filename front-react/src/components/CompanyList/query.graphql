query companies($search: String!) {
    companies(search: $search) {
        items(limit:10, offset: 0) {
            id
            name
            products {
                name
                price
            }
        }
        count
    }
}
